###THIS is old version goto samsit-phew/catty-player or 
```bash
cargo install catty-player
```
or if ya wanna copy paste
```bash
git clone git@github.com:samsit-phew/catty-player.git & cd catty-player & cargo run --release
```
if ya want to install globally (the git version)
```bash
cargo install --path
```
be sure to be on that directory to install that
